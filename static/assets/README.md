# Managed assets

`MANIFEST` is the canonical inventory for pipeline-managed assets.

The manifest's `schema_version` is currently `1`. Each managed asset records
its shipped and fetched byte hashes, dimensions, author, source, licence,
retrieval date, and transform. The existing demo assets in this directory are
not retroactively asserted to be pipeline-managed.

The Manhattan CC0 scenery is pipeline-managed.
